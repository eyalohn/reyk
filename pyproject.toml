[project]
name = "pyisolate"
version = "0.1.0"
description = "TODO"
readme = "README.md"
requires-python = ">=3.12"
dependencies = []

[tool.uv]
dev-dependencies = [
    "pytest>=8.4.2",
]

[tool.setuptools.packages.find]
include = ["pyisolate"]

[tool.uv.workspace]
members = [
    "cli",
]

[tool.uv.sources]
pyisolate-cli = { workspace = true }

[project.optional-dependencies]
cli = [
    "pyisolate-cli",
]

[project.scripts]
pyisolate = "pyisolate.cli:main"
