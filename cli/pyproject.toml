[project]
name = "pyisolate-cli"
version = "0.1.0"
description = "Install vendored packages for pyisolate."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "typer>=0.20.0",
]

[project.scripts]
pyisolate-cli = "main:app"
